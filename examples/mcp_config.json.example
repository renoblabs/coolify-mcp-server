{
  "mcpServers": {
    "coolify-remote": {
      "comment": "Remote HTTP/SSE for mobile/remote clients (recommended)",
      "endpoint": "https://mcp.your-domain.com",
      "transport": "sse",
      "authentication": {
        "type": "bearer",
        "token": "YOUR_MCP_AUTH_TOKEN_FROM_DOPPLER"
      }
    }
  }
}
